Ces composants composent l’interface utilisateur du frontend DroneWeb. Ils sont organisés en trois grandes catégories :

    common/ → composants génériques réutilisables

    flights/ → composants métier liés à l’affichage d’un vol/détection

    layout/ → composants structurants qui organisent la page

Chaque composant a été revu pour être :

    Lisible

    Facilement maintenable

    Compatible production (pas de logs inutiles, accessibilité améliorée)

    Stylistiquement cohérent avec App.css

1. Dossier components/common/
FlightMap.tsx

    Rôle : Affiche une carte Leaflet avec la trajectoire (trace) d’un vol.

    Props clés :

        trace (LatLngExpression[]) → liste de coordonnées [lat, lon].

        markerIcon (L.Icon) → icône pour marquer la position finale.

        zoom (number) → niveau de zoom initial.

        showMarkers (bool) → afficher des cercles aux points de la trace.

        polylineOptions → personnalisation de la ligne.

    Utilisation : Utilisé dans MapLayout et AnchorModalLayout.

    Important :

        Utilise InvalidateMapSize pour éviter bug de rendu après resize.

        Couleurs personnalisables via props ou CSS pour uniformité.

FlyToPosition.tsx

    Rôle : Déplace/centre la carte Leaflet vers une position donnée.

    Props clés :

        position (LatLngTuple | null) → coordonnées de destination.

        zoom (optionnel) → niveau de zoom après déplacement.

        duration → durée en secondes de l’animation.

        debug → activer/désactiver logs.

    Utilisation : Permet le suivi live d’un drone sur la carte.

Pagination.tsx

    Rôle : Navigation simple entre les pages de données.

    Props clés :

        page → page courante (commence à 1).

        maxPage → nombre total de pages.

        setPage → callback pour changer de page.

    Utilisation : Le plus souvent dans TablesLayout.

    Accessibilité : Gestion des aria-label et aria-live pour lecteurs d’écran.

2. Dossier components/flights/
DetailsPanel.tsx

    Rôle : Affiche les détails d’un vol sélectionné.

    Props clés :

        selected (Flight | null)

        detailFields → liste des champs à afficher.

        exportObj → fonction pour exporter le JSON du vol.

        selectedTracePoints → points du tracé à afficher.

        selectedTraceRaw → données brutes JSON du tracé.

    Fonctions :

        Trie les champs (IMPORTANT_FIELDS en premier).

        Affiche soit texte soit objet JSON formaté.

        Affiche liste des points (local/API).

    Utilisation : intégré dans MapLayout.

DetectionsTable.tsx

    Rôle : Table générique pour afficher une liste de détections/vols avec possibilité d’ancrer.

    Props clés :

        data → liste des détections/vols.

        fields → colonnes à afficher.

        onSelect → callback quand l’utilisateur clique sur une ligne.

        isAnchored → savoir si un vol est déjà ancré.

        anchorFlight → action pour ancrer un vol.

        renderAnchorCell → personnalisation visuelle de la cellule d’ancrage.

    Accessibilité :

        Navigation clavier (tabIndex, onKeyDown).

        aria-label sur boutons.

3. Dossier components/layout/
AnchorModalLayout.tsx

    Rôle : Modal affichée lors de la préparation d’un ancrage.

    Contenu :

        JSON des données.

        Champ description obligatoire.

        Carte (FlightMap) avec possibilité de déplacer/zoomer.

        Boutons "Valider" / "Annuler".

    Props clés :

        anchorModal (AnchorModal) → données du vol.

        anchorDescription / setAnchorDescription

        isZipping → état de préparation de l’archive ZIP.

        onValidate / onCancel

    Protection : Empêche validation si description vide ou trace absente.

    Accessibilité : role="dialog", aria-modal.

Header.tsx

    Rôle : Bandeau supérieur de l’application avec icône et titre.

    Props clés :

        title → par défaut "DroneWeb".

        debug → logs en console conditionnels.

    Accessibilité :

        role="banner", titre <h1>.

MapLayout.tsx

    Rôle : Colonne gauche principale → Carte + panneau détails.

    Composition :

        Titre

        FlightMap pour l’affichage

        DetailsPanel en-dessous

    Props clés :

        selected, selectedTracePoints, selectedTraceRaw, detailFields, exportObj

    Utilisation : Directement instancié dans App.tsx.

Sidebar.tsx

    Rôle : Zone latérale optionnelle pour filtres, menus ou info.

    Props clés :

        children → contenu à afficher.

        title optionnel.

        debug → logs contrôlés.

    Accessibilité : role="complementary".

TablesLayout.tsx

    Rôle : Section droite → Tableau live, tableau local, tableau API avec pagination.

    Composition :

        Erreur globale (error)

        Tableau live (drones actuels)

        Tableau local (historique local avec pagination et ancrage)

        Tableau API (historique distant avec pagination)

    Props clés :

        drones / LIVE_FIELDS

        localPageData / setLocalPage

        apiPageData / setApiPage

        isAnchored / renderAnchorCell

        HISTORY_API_FIELDS

        handleSelect

    Accessibilité :

        scope="col" sur colonnes

        Messages pour tables vides

    Facteur DRY : Possible de factoriser futur code en un composant DataTable.

🛠 Bonnes pratiques maintenabilité

    Garder les props bien typées (Flight, Event, etc.) via types/models.ts.

    Centraliser les styles dans App.css ou un CSS module pour éviter styles inline.

    Utiliser debug pour contrôler les logs sans commenter du code.

    Factoriser au besoin : si deux tables ou deux panneaux se ressemblent beaucoup, créer un composant commun.

    Respecter l’accessibilité : roles, aria-label, aria-live, clavier.

🔄 Flux de données entre composants

    App.tsx est l’orchestrateur → il appelle :

        useDrones → données live vers TablesLayout et useLiveTraces

        useLocalHistory / useAnchored / useRemoteEvents

        Passe selected et trace à MapLayout

    Interactions :

        TablesLayout → handleSelect → met à jour selected → passé à MapLayout → DetailsPanel

        renderAnchorCell de TablesLayout ou DetectionsTable → ouvre AnchorModalLayout

        AnchorModalLayout → onValidate → envoi backend

    FlightMap est réutilisé dans carte principale et modale.
