📚 Documentation technique – Backend DroneWeb
1. Présentation générale

Le backend DroneWeb est une application Node.js/Express qui :

    Gère l’historique des vols (fichiers JSON locaux)

    Gère l’ancrage blockchain (vol + preuve ZIP horodatés)

    Sert de proxy vers l’API GraphQL distante des drones

    Charge sa configuration depuis un fichier .env

    Dispose d’un système de logs centralisé filtrable (LOG_LEVEL)

    Implémente une gestion centralisée des erreurs & routes inexistantes

2. Architecture du projet

text
📂 backend/
│
├── 📂 routes/                 # Les groupes d’API Express
│   ├── history.js              # Routes pour l’historique des vols
│   ├── anchor.js               # Routes pour les ancrages
│   ├── graphql.js              # Routes proxy GraphQL
│   └── index.js                # Routeur global
│
├── 📂 services/               # Logique métier (pas de code Express ici)
│   ├── flightService.js        # Gestion historique vols
│   ├── anchorService.js        # Gestion ancrages
│
├── 📂 middlewares/            # Middlewares Express
│   ├── notFoundHandler.js      # Gestion 404
│   └── errorHandler.js         # Gestion d’erreurs globale
│
├── 📂 utils/
│   └── logger.js               # Logger centralisé filtrable par LOG_LEVEL
│
├── server.js                   # Point d’entrée de l’application
├── .env / .env.dev / .env.prod # Configurations
└── package.json

3. Fonctionnement interne
3.1 server.js

    Charge les variables .env

    Configure CORS, parsing JSON, TLS (désactivable pour le dev)

    Monte routes/index.js

    Positionne :

        notFoundHandler → 404

        errorHandler → erreurs serveur

    Lance l’écoute sur BACKEND_PORT

3.2 Les routes

    Minimalistes : elles définissent les chemins et délèguent au service associé.

    Log chaque requête reçue via le logger centralisé (log()).

    Ne contiennent pas de logique métier.

Exemple (/routes/history.js) :

js
router.get('/history', async (req, res, next) => {
  log('debug', `→ GET /history depuis ${req.ip}`);
  try {
    await flightsService.handleGetHistory(req, res);
  } catch (error) {
    next(error);
  }
});

3.3 Les services

flightService.js

    Lecture/écriture dans flights_history.json

    Ajout en lot / unitaire

    Export d’un vol par ID et date

    Évite les doublons

    Tous les logs filtrables (info, debug, warn, error)

anchorService.js

    Lecture/écriture anchored.json

    Sauvegarde dans dossier daté :
    ancrage.json + preuve.zip

    Vérifie les doublons d’ancrages

💡 tokenService.js supprimé dans cette version car /graphql est accessible sans authentification.
3.4 Middlewares

middlewares/notFoundHandler.js

    Retourne un 404 avec un log warn incluant la méthode, l’URL et l’IP.

middlewares/errorHandler.js

    Retourne une réponse JSON uniforme :

json
{
  "success": false,
  "error": "Message d'erreur",
  "errorStack": "..." // seulement en dev
}

    Enregistre l’erreur en fonction du niveau.

3.5 Logger centralisé (utils/logger.js)

    Définit les niveaux : error > warn > info > debug

    Filtre l’affichage selon LOG_LEVEL dans .env

    Format :

text
[INFO] 2025-08-14T08:22:33.123Z - Message

Exemple :

js
log('info', 'Historique chargé avec 32 vols');
log('debug', `Requête entrante ${req.method} ${req.url}`);
log('error', `Erreur lors de l’écriture : ${error.message}`);

4. Configuration .env
Variables disponibles :

text
##############################################
# SERVEUR
##############################################
BACKEND_PORT=3200
CORS_ORIGIN=*          # En prod → domaine exact
MAX_JSON_SIZE=5mb
IGNORE_TLS_ERRORS=false
NODE_ENV=development
LOG_LEVEL=debug        # debug | info | warn | error

##############################################
# API DRONE DISTANTE (GraphQL)
##############################################
API_PROTOCOL=http
API_HOST=192.168.1.105
API_PORT=3200

##############################################
# STOCKAGE LOCAL
##############################################
FLIGHTS_HISTORY_FILE=flights_history.json
ANCHOR_FILE=anchored.json
ANCHORED_DIR=anchored

##############################################
# BLOCKCHAIN (optionnel)
##############################################
BLOCKCHAIN_API_URL=
BLOCKCHAIN_API_KEY=

5. Bonnes pratiques internes

    Toute logique métier → services/

    Aucune valeur en dur → passer par .env

    Logs normalisés :

        info : événements importants

        debug : bruit de debug (masqué en prod)

        warn : anomalies non bloquantes

        error : échec bloquant

    Gestion unique des erreurs :

        lever des exceptions dans les services

        propagation avec next(error) dans les routes

    Pas de console.log() brut : toujours passer par le logger

6. Lancement & Dev Workflow

Installation :

bash
cd backend
npm install

Lancement en dev :

bash
LOG_LEVEL=debug npm start

Lancement en prod :

bash
NODE_ENV=production LOG_LEVEL=info npm start

Flux de dev recommandé :

    Créer une nouvelle fonctionnalité → ajouter/mettre à jour un service

    Créer la route associée

    Importer dans routes/index.js

    Tester via Postman / frontend

    Committer et passer en revue

7. Historique des refactos par rapport à l'ancien backend

    Suppression tokenService.js → pas de token requis pour /graphql

    Suppression dépendances inutiles (body-parser, graphql)

    Centralisation du logging → utils/logger.js

    Réduction massive du bruit console

    Passages systématiques par LOG_LEVEL pour filtrer

    Uniformisation des formats de logs

    Séparation stricte logique métier / routes

    .env nettoyé → suppression variables inutilisées (LOGIN, PASSWORD, etc.)

